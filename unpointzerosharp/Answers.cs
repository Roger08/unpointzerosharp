using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace unpointzerosharp
{
    internal static class Answers
    {
        internal enum AnswerMethods {
            Login,
            Register,
            NewCharacter,
        };

        public static void Login(byte index, byte[] data)
        {
            //A faire
        }

        public static void Register(byte index, byte[] data)
        {
            //A faire
        }

        public static void NewCharacter(byte index, byte[] data)
        {
            //A faire
        }
    }
}
